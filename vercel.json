{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "echo Static deploy — skipping npm install",
  "buildCommand": "echo Static deploy — no build required",
  "outputDirectory": "docs",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
